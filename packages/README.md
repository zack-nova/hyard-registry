# Namespaced Package Handles

Ordinary public Package Handles live here.

```text
packages/<namespace>/index.yaml
```

The namespace is recorded in `namespaces/<namespace>.yaml` as one or more
Git-platform user or organization identities. Each package entry in a namespace
index records package-level status, dist-tags, version locator metadata, and
validation evidence.
